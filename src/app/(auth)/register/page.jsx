import RegisterForm from "./RegisterForm";

export const metadata = {
  title: "Register | Mediqueue",
  description: "Create a new Mediqueue account to find and book top tutors in Bangladesh.",
};

const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;
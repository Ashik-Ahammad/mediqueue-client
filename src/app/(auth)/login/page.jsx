import LoginForm from "./LoginForm";

export const metadata = {
  title: "Login | Mediqueue",
  description: "Sign in to your Mediqueue account to book sessions and manage your profile.",
};

const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;